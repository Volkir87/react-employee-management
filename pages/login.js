import axios from 'axios';

const Login = () => {
    let login = (user, password) => {
        axios.get();
    }
    return (
    <div>
      <p>This is the login page</p>

    </div>
  )}

  export default Login